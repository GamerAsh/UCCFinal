<div class="leftcol"
<h2>Password reminder</h2>

<% form_for(:user, :url => recover_path) do |f| %>
    <div class="field">
      <%= f.label :email %>
      <br/>
      <%= f.text_field :email %>
    </div>

    <div class="actions">
      <%= f.submit "Recover" %>
    </div>
<% end %>
</div>